{
  "name": "chat",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rimraf ./dist",
    "dev": "npm run clean && parcel ./src/**/index.pug -p 3000",
    "build": "parcel build ./src/**/index.pug",
    "start": "npm run clean && npm run build && node server.js"
  },
  "license": "MIT",
  "devDependencies": {
    "@parcel/config-default": "^2.8.3",
    "@parcel/transformer-pug": "^2.8.3",
    "@parcel/transformer-sass": "^2.8.3",
    "parcel": "^2.8.3",
    "parcel-transformer-pug-precompile": "^1.0.6",
    "pug": "^3.0.2",
    "rimraf": "^4.1.2"
  },
  "keywords": [],
  "dependencies": {
    "express": "^4.17.2"
  }
}
